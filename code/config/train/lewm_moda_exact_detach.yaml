defaults:
  - lewm
  - _self_

output_model_name: lewm_moda64_exact_detach
subdir: pusht_moda64_exact_detach

wandb:
  enabled: false

use_moda: true
depth_start_layer: 1
cache_window: 0
detach_cache: true

predictor:
  depth: 64
  dropout: 0.0
